
export class SkuImage {
    constructor(
      public id?: number,
      public url?: string,
      public name?: string,
      // public store?: Store,
      public created_at?: Date,
      public updated_at?: Date
    ) {}
}